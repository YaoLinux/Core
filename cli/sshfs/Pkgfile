makedepends=(fuse3 glib openssh)

description="Contains a filesystem client based on the SSH File Transfer Protocol"
url="http://fuse.sourceforge.net/sshfs.html"

packager="Yaolinux Team"
maintainer="rems"

name=sshfs
version=3.7.0

source=(https://github.com/libfuse/$name/releases/download/$name-$version/$name-$version.tar.xz)

build=meson
