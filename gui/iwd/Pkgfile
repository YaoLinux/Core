#Depends on: dbus glibc

description="Internet Wireless Daemon" 
url="https://git.kernel.org/cgit/network/wireless/iwd.git/"
maintainer="Dania <Dania@nutyx.org>"
packager="YaoLinux Teams"

name=iwd
version=0.17

source=(https://www.kernel.org/pub/linux/network/wireless/iwd-${version}.tar.xz
        iwd.tmpfiles)

build() {
  cd $name-$version

      ./configure --prefix=/usr \
	--sysconfdir=/etc \
	--libexecdir=/usr/lib/iwd \
        --enable-wired \
	--localstatedir=/var
  make
}

package() {
   cd $name-$version
   make install DESTDIR=$PKG
   install -Dm644 $SRC/iwd.tmpfiles $PKG/usr/lib/tmpfiles.d/iwd.conf

}

