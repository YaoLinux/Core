makedepends=(mesa libepoxy)

description="A virtual 3D GPU library, allows guest operating system to use the host GPU to accelerate 3D rendering"
url="https://virgil3d.github.io/"

packager="Yaolinux Team"
maintainer="rems"

name=virglrenderer
version=0.8.0
release=2

PKGMK_KEEP_SOURCES="no"

source=(https://github.com/freedesktop/virglrenderer/archive/$version.tar.gz)

prepare() {

cd $name-$version

./autogen.sh
}
