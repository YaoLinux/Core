makedepends=(freefont-ttf iso-codes itstool gtk3 docbook-xml libseccomp gnome-doc-utils gsettings-desktop-schemas gobject-introspection startup-notification xorg-xkeyboard-config)

description="Library with common API for various GNOME modules"
url="http://www.gnome.org/"

packager="Yaolinux Team"
maintainer="rems"

run=(gsettings-desktop-schemas freefont-ttf)

name=gnome-desktop
version=3.38.2

source=(http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz)

build() {

 mkdir -v build && cd build

 meson ../$name-$version \
       --prefix=/usr \
       -Dgnome_distributor="YaoLinuX"

 ninja
 DESTDIR=$PKG ninja install
}