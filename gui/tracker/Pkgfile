makedepends=(json-glib icu networkmanager python2 sqlite upower libsoup vala gtk-doc desktop-file-utils gtk-update-icon-cache libseccomp)

description="Semantic data storage"
url="http://www.gnome.org"

packager="Yaolinux Team"
maintainer="rems"

run=(desktop-file-utils gtk-update-icon-cache)

name=tracker
version=2.3.0
release=2

source=(http://ftp.gnome.org/pub/gnome/sources/$name/${version%.*}/$name-$version.tar.xz)

build=meson
